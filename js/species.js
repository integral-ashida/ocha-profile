const params=new URLSearchParams(location.search),selected=SPECIES.find(s=>s.id===params.get('id'))||SPECIES[0],title=document.querySelector('#species-name'),latin=document.querySelector('#species-latin'),hero=document.querySelector('#species-hero'),description=document.querySelector('#species-description'),gallery=document.querySelector('#species-gallery'),indexList=document.querySelector('#species-index-list'),indexSearch=document.querySelector('#species-index-search');document.title=`${selected.name}｜日本の主なゲンゴロウ`;title.textContent=selected.name;latin.textContent=selected.latin;latin.hidden=!selected.latin;hero.src=selected.image;hero.alt=selected.name;description.textContent=selected.description||'編集中、もうしばらくお待ちください';gallery.innerHTML=selected.gallery.map((src,i)=>`<img src="${src}" alt="${selected.name}の写真 ${i+1}" loading="lazy">`).join('');function renderIndex(q=''){const term=q.trim().toLowerCase();indexList.innerHTML=SPECIES.filter(s=>s.name.toLowerCase().includes(term)).map(s=>`<a href="species.html?id=${s.id}" class="${s.id===selected.id?'current':''}">${s.name}</a>`).join('')||'<p>MM</p>'}indexSearch.addEventListener('input',e=>renderIndex(e.target.value));renderIndex();
